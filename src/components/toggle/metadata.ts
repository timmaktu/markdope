import type { ComponentMetadata } from "../../registry/types";

export const toggleMetadata: ComponentMetadata = {
	id: "official.toggle",
	namespace: "official",
	slug: "toggle",
	version: 1,
	displayName: "Toggle",
	description: "A labeled on/off switch that can update YAML directly from the rendered view.",
	supportsVisualEditing: true
};

